<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.label :ФИО %><br>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :Текст %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit :Создать %>
  </p>
<% end %>